# Caddyfile для production-стека EduQuiz.
# Auto-TLS via Let's Encrypt. Домен берётся из переменной окружения DOMAIN.
#
# Маршрутизация:
#   /api/*    → api:8080  (REST)
#   /ws/*     → api:8080  (WebSocket)
#   /healthz, /readyz → api:8080  (служебные)
#   /*        → /srv/web  (Flutter Web SPA, fallback на index.html)

{
	# Глобальные опции
	email admin@{$DOMAIN}
	servers {
		protocols h1 h2 h3
	}
}

{$DOMAIN} {
	encode zstd gzip

	# Безопасность по умолчанию
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
		X-Frame-Options "DENY"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "geolocation=(), microphone=(), camera=()"
		-Server
	}

	# WebSocket-эндпоинты
	handle /ws/* {
		reverse_proxy api:8080
	}

	# REST API (без strip_prefix — API ожидает /api/v1/...)
	handle /api/* {
		reverse_proxy api:8080 {
			header_up Host {host}
			header_up X-Real-IP {remote_host}
		}
	}

	# Health-эндпоинты
	handle /healthz {
		reverse_proxy api:8080
	}
	handle /readyz {
		reverse_proxy api:8080
	}

	# Загруженные файлы (картинки к вопросам)
	handle /files/* {
		uri strip_prefix /files
		root * /srv/uploads
		file_server
	}

	# Flutter Web SPA
	handle {
		root * /srv/web
		try_files {path} /index.html
		file_server
	}

	log {
		output stdout
		format console
	}
}
