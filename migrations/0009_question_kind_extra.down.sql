-- Удаление значений из ENUM в Postgres напрямую невозможно — нужно
-- пересоздавать тип. Оставляем no-op: down-migration не удаляет
-- 'numerical'/'true_false', но это безопасно — они не нарушают логику
-- старых типов.
SELECT 1;
